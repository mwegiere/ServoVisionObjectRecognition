# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
#ADD_COMPONENT(HomogMatrixPub)

ADD_COMPONENT(DrawDiods)

ADD_COMPONENT(DiodRecognition)

ADD_COMPONENT(NewestImage)

#ADD_COMPONENT(TimeCount)

ADD_COMPONENT(HomogMatrixPub)

ADD_COMPONENT(ROSProxy)
